import 'package:flutter/material.dart';

/// Stub — CurrentMissionBanner decommissioned with the old node system.
/// Replaced by the new hub widget in Task 5.
class CurrentMissionBanner extends StatelessWidget {
  const CurrentMissionBanner({super.key});

  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
