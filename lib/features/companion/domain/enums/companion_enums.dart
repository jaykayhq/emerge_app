enum CompanionEventType {
  firstFeatureVisit,
  milestoneReached,
  struggleDetected,
  featureUnlocked,
  dailyCheckIn,
  userInitiated,
}

enum CompanionMode { overlay, panel, inlineCard }
