Standalone HTML5 pages styled exclusively with Tailwind CSS loaded from `cdn.jsdelivr.net` (with `forms` and `container-queries` plugins), Google Fonts (Spline Sans and Plus Jakarta Sans), and Material Symbols Outlined icons. Dark mode is implemented via Tailwind's `class` strategy with a `dark` class on the `<html>` element. No JavaScript framework, bundler, or build step is used.