{
  "name": "emerge-email-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Cron email worker for Emerge (welcome, re-engagement, verification grace) — runs in GitHub Actions, sends via SMTP (no Resend).",
  "type": "module",
  "engines": {
    "node": "22"
  },
  "scripts": {
    "start": "node src/index.js",
    "test": "node --test test/"
  },
  "dependencies": {
    "firebase-admin": "^13.10.0",
    "nodemailer": "^7.0.0"
  }
}
